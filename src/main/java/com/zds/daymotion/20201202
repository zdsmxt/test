














git status

git fetch

git merge origin/feature/esutil


origin 远程

20000 两万的返回标识 操作成功  弃用


测试的问题 描述 由于我是手动添加的角色信息 没有随着选择插入角色权限码关联表至少一条数据 导致的bug 插入的有空字符串



 if(null!=pid&&!pid.equals("")){
                rp.setPid(Integer.parseInt(pid));
            }


标签名不能重复

实体名也不能重复

权限依赖

标签表里面添加 权限名称索引










我昨天发的那个数据库设计你好好看看，
有什么疑问尽早提出来。前三张信息表对平台来说只有查询操作，
更新操作是由数仓来做；后两张表是本次功能的核心表，
curd接口都要有。没什么疑问就开始建表、实体、services










目前系统的四种角色 和可读写的范围
超级管理员 首页（读） 标签管理（读写） 标签应用（读写） 系统管理（读写）
标签管理员 首页（读） 标签管理（读写） 标签应用 (读)
访客      首页（读） 标签应用(具体标签分类下的标签详情页不能看)
业务人员   首页（读） 标签应用（读）（都可看）


角色           拥有的权限码
admin         所有权限码

tagAdmin      除权限 角色 用户以外的所有权限码

visitor       home

business


权限 有公开
六大部门都有
用户 有 未分配



****************************备注********************
部门表名称 唯一索引约束
角色英文名称 唯一索引约束
标签名称 唯一索引约束


对于下拉菜单的特殊菜单 后端 返回的时候 靠前返回
在语句中 把值写死的修改 status


            /**
             * 不用lamada表达式的写法
             * <hr><blockquote><pre>
             * for (int i = 0; i < allDept.size(); i++) {
             *     if(allDept.get(i).getName().equals(UNALLOCATED)){
             *         allDept.remove(i);
             *      }
             * }
             * </pre></blockquote><hr>
             */




对于新的分支

创建jira 单 根据单号创建属于自己的开发分支

最终合到 第二期








  20201202daylog
        一 工作进度：
        1.变更用户的启用状态 查看是否有分页逻辑
        2.测试不同角色 可以查看的页面
        3.修改用户那块响应的消息体存到data的情况
        4.开发二期企业 企业主 员工 查询接口



        二 存在的疑问：
        1.用户角色 访客看不到标签管理的模块

